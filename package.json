{
  "name": "habit-spark",
  "version": "0.1.0",
  "private": true,
  "description": "Tiny habit tracker scaffold for Codex Lab",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "test": "npm run build && node dist/smoke-test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
